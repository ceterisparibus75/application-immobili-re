# === Base de données (Supabase) ===
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# === Supabase ===
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""

# === NextAuth v5 ===
AUTH_SECRET="" # openssl rand -base64 32
AUTH_URL="http://localhost:3000" # En production : https://votre-domaine.com

# === Chiffrement données sensibles (IBAN/BIC) ===
ENCRYPTION_KEY="" # 32 bytes en base64

# === Resend (emails) ===
RESEND_API_KEY=""
EMAIL_FROM="gestion@votredomaine.fr"

# === API INSEE ===
INSEE_API_KEY=""
INSEE_API_SECRET=""

# === Configuration applicative ===
NEXT_PUBLIC_APP_NAME="Gestion Immobilière"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# === Storage Supabase ===
SUPABASE_STORAGE_BUCKET="documents"

# === Cron ===
CRON_SECRET=""

# === IA (analyse de documents) — optionnel ===
ANTHROPIC_API_KEY=""

# === GoCardless (connexion bancaire) — optionnel ===
GOCARDLESS_SECRET_ID=""
GOCARDLESS_SECRET_KEY=""

# === Upstash Redis (rate limiting) — optionnel ===
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# === Stripe (abonnements) — optionnel ===
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
STRIPE_PRICE_ID_STARTER_MONTHLY=""
STRIPE_PRICE_ID_STARTER_YEARLY=""
STRIPE_PRICE_ID_PRO_MONTHLY=""
STRIPE_PRICE_ID_PRO_YEARLY=""
STRIPE_PRICE_ID_ENTERPRISE_MONTHLY=""
STRIPE_PRICE_ID_ENTERPRISE_YEARLY=""

# === Powens / Budget Insight (connexion bancaire) — optionnel ===
POWENS_DOMAIN=""
POWENS_CLIENT_ID=""
POWENS_CLIENT_SECRET=""

# === DocuSign (signature électronique) — optionnel ===
DOCUSIGN_INTEGRATION_KEY=""
DOCUSIGN_SECRET_KEY=""
DOCUSIGN_ACCOUNT_ID=""
DOCUSIGN_BASE_URL=""

# === Sentry (monitoring) — optionnel ===
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_ORG=""
SENTRY_PROJECT=""

# === Zendesk Widget — optionnel ===
NEXT_PUBLIC_ZENDESK_KEY=""
